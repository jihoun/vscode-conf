// Place your key bindings in this file to overwrite the defaults
[
    { "key": "ctrl+cmd",          "command": "editor.action.triggerSuggest",
                                     "when": "editorHasCompletionItemProvider && editorTextFocus && !editorReadonly" }
]